/**
 * Root page — renders the TGDetect landing page.
 * The landing page implementation lives in src/app/landing/page.tsx.
 */
'use client';

import LandingPage from './landing/page';

export default function Page() {
  return <LandingPage />;
}
